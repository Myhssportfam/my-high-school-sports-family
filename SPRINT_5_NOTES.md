# Sprint 5 — Professional National Map

## Completed
- Replaced the hex-tile map with a recognizable United States silhouette.
- Kept all 50 state communities clickable.
- Added state abbreviations, hover/focus effects, live-activity heat pulses, and a state status panel.
- Preserved existing state routes, Firebase files, athlete pages, recruiting, live center, and gaming arena.
- Upgraded the header/footer brand mark to display MHSSF.
- Added responsive mobile behavior.

## Run locally
```powershell
npm install
npm run dev
```
Then visit `http://localhost:3000`.

## Build verification
The automated dependency installation timed out in the build environment, so run `npm install` and `npm run build` locally to verify the production build.
